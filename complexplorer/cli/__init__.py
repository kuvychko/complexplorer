"""Command-line interface for Complexplorer.

The console entry point is ``complexplorer.cli.main:main``. Import the function as
``from complexplorer.cli.main import main`` (not re-exported here, to avoid shadowing the
``main`` submodule with the ``main`` function).
"""
