# Complexplorer rev3 closeout

Status: release candidate closeout plan  
Reviewed branch: `docs/openspec-baseline`  
Reviewed commit: `e63696a31dd9b9533fda9e65f241165aee3f89fd`

## Goal

Ship Complexplorer 3.0 as a coherent, trustworthy visualization library—not merely a
collection of finished features.

Rev3 already contains enough substantive work for a major release: a single PyVista 3D
backend, a shared surface kernel, Riemann surfaces, the function catalog and CLI, engineering
mode, a phase-wheel legend, and a smaller public API. The remaining work should be a feature
freeze focused on four outcomes:

1. **Show the library convincingly.** A visitor should understand the range and quality of
   rev3 within 30 seconds, before reading the API.
2. **Teach the supported workflows.** A new user should be able to reproduce every flagship
   image from a short, current example.
3. **Make the release contract honest.** Version, changelog, migration guidance, packaging,
   CI, and published documentation must agree.
4. **Remove visible scaffolding.** Public prose and metadata should describe the product that
   exists, not internal phases, future consumers, or obsolete backend plans.

## Release definition of done

Rev3 is ready when all of the following are true:

- [ ] The README opens with a representative rev3 visual—not a single legacy image—and a
      one-sentence description broad enough to include mathematical, engineering, and physical
      output.
- [ ] A curated visual tour demonstrates 2D portraits, phase-wheel legend, analytic landscape,
      Riemann sphere/relief, multivalued Riemann surface, engineering mode, and STL/physical
      output.
- [ ] Every flagship visual has a short runnable example and is generated by a documented
      command.
- [ ] Current, hosted rev3 documentation is ready to deploy with the package.
- [ ] A task-oriented 2.x → 3.0 migration guide covers every intentional break.
- [ ] The wheel and sdist—not only an editable checkout—pass installation and rendering smoke
      tests.
- [ ] Claimed Python versions and minimum dependency versions are enforced in CI.
- [ ] The final release commit passes tests, lint, formatting, OpenSpec validation, notebook
      execution, gallery regeneration, and artifact checks.
- [ ] Changelog date, tag, GitHub release, PyPI package, and hosted docs describe the same
      released version.

## Closeout finding: the published 2.0.0 baseline (added 2026-09-14)

PyPI carries **2.0.0** (uploaded 2025-10-19), built from tag `v2.0.0` on `main`. This branch
forked from `main` at `c892bdb` (2025-08-22, just after 1.0.1), so it has never contained 2.0.
Measured against what users actually have installed, rev3 removes:

- 9 colormaps (`OklabPhase`, `PerceptualPastel`, `AnalogousWedge`, `DivergingWarmCool`,
  `Isoluminant`, `CubehelixPhase`, `InkPaper`, `EarthTopographic`, `FourQuadrant`, plus OkLCh
  gamut clipping)
- the published `n_phi → phase_sectors` rename
- the logging API
- 12 exception classes
- `show` and the `*_preset` helpers
- the MkDocs Material site, live on `gh-pages`
- 12 notebooks, including `05_accessibility_cvd`

The CHANGELOG claim that 2.0 was "never published" is false.

**Decisions:**
- Rev3 stays the trunk, and assets worth keeping are ported selectively.
- `phase_sectors` is canonical.
- The migration guide covers **2.0.0 → 3.0**.
- Histories are joined at release with `git merge -s ours origin/main`.
- Visual review runs through a private review page.
- Printed-ornament photos are supplied by the owner.

## Visual review outcomes

### R0 — style calibration (decided 2026-09-15)

Review board: https://claude.ai/artifact/L8avJ54troJqabjh8iERHw

**Profile B ("Gallery grey") is the house style for every render family**, and it is the locked
`RENDER_PROFILES` baseline for `curate-rev3-visual-tour`:

- ground: vertical gradient `#fbfcfd` (top) to `#e6e9ee` (bottom)
- lighting: three-point, specular `0.3`
- finish: SSAA, no orientation widget, camera zoom `1.22` (landscape `0.94`, so the domain
  corners stay inside the frame)
- window: 1560 x 1560 px
- 2D portraits: axes and ticks kept, phase-wheel legend inset, tight bounding box
- camera: every render refits the camera to its subject before zooming, so framing is
  consistent per family (landscape zoom 0.95 — above 1.0 the domain corners clip)
- log Riemann surfaces render at `r_max=3.0` from (2.5, 2.5, 3.5)
- page images are served as 400 px `thumb/` and 900 px `view/` derivatives (6.8 MB per page
  instead of 45 MB); each figure links its full render

### R0b — resolution (decided 2026-09-16)

Owner note from R0: "We need to increase the sphere's resolution - the figure is blocky. Main
benefit of PyVista is excellent visual quality, we don't want to lose it in the gallery - where it
matters most." A ladder per family settled it; cost is ~1 s and a few hundred KB at every step, so
the choice was purely about quality.

| Family | Resolution | Was |
|---|---|---|
| 2D portrait (samples per axis) | **1200** | 400 |
| Riemann sphere | **1000** | 260 |
| Relief / ornament | **800** | 260 |
| Analytic landscape | **600** | 260 |
| Riemann surface | **312** | 104 |

The portrait ladder was added during R0b after the fix to the clipped labels exposed a second
defect: portraits sampled the domain at 400 x 400 while being saved near 1500 px, so they were
upscaled about 3.75x and their phase-sector boundaries stair-stepped. `generate_gallery` now takes
a `resolution` argument, defaulting to one sample per output pixel.

### R1 — curated tour (approved 2026-09-17)

All eight tour assets approved: legend portrait, portrait-to-landscape pair, engineering figure
(notch filter), composition proof, composite domain, sphere-vs-surface, physical triptych, and the
orbit loop.

Owner note on the first pass: *"The images look good - but the text (axis labels, any text) is
blurry. let's make sure we are not downsampling somewhere."* Correct on both counts:

- the composition step pasted finished panels into a 760 px cell, halving matplotlib's already
  rasterised text. Panels are now rendered AT their final size (`CELL = 1200`) and pasted 1:1;
  the Bode panel is rendered at the grid's exact width.
- the review page itself showed each ~3700 px asset through a 400 px gallery thumbnail; its cards
  now use 1400 px previews.

### R4 — in context (2026-09-17)

Checked on the pushed branch: the hero, the section figures and the collapsed snippets render on
GitHub, and the table-of-contents anchors resolve. Found there and fixed: the page pulled about
45 MB because figures above thumbnail width served full renders.

### R2 — hero montage (decided 2026-09-17)

**Labels below the panels**, chosen over the band-on-panel variant: the art stays uninterrupted.
The montage is ~1972 x 1406 px, so it holds up at GitHub's ~830 px README column, and every panel
is trimmed to its subject (untrimmed, the 3D panels carried wide margins that made the surface and
landscape look small). The alternative layout remains available in `examples/tour.py`, but only
the chosen one is produced and committed.

### R3 — full regeneration (2026-09-17)

All six sections approved. Riemann surfaces was sent back once: *"log * surface is too skinny - increasing
the diameter by at least 2x will help."*

The helicoid's height is `2*pi*turns` (about 19) while its radius defaulted to 1.5, so it was six
times taller than wide. It now renders at `r_max=3.0` (double the diameter, the point past which
the upper sheets simply occlude the lower ones) from a camera that shows the ramp turning through
all three levels, and was approved on the re-look.

**Physical output:** the author supplied a photograph of the printed pole-flower ornament and a
captioned composite of the relief beside it. Both are curated assets; the photograph now fills the
hero's physical panel and the third panel of the triptych, replacing the clay-mesh placeholder.

### CI finding (2026-09-17)

The `Phase` golden fixture was recorded on Windows and compared with `np.array_equal`, so it
failed all ten cases on Linux CI while the local difference was exactly 0.0 — platform-level
last-bit variation in the trigonometric and logarithmic work, not a change in `Phase`. The
comparison now allows half an 8-bit level (1/512), below which a saved PNG pixel cannot change.
Rendered pixel data is environment-sensitive; only the manifests are byte-stable.

**Defects found during R0, owned by `curate-rev3-visual-tour`:**

- The committed gallery portraits clip the `Im(z)` axis label at the left edge and carry a wide top
  margin; `cp.gallery`'s save call needs a tight bounding box.
- Per-vertex colouring on the rectangular sphere mesh stair-steps where a brightness band crosses
  mesh rows (the R0b subject).

## Proposal map

The plan's sections map onto these OpenSpec changes (status tracked in `ROADMAP.md`):

| Change | Covers |
|---|---|
| `reconcile-with-2-0-release` (C1) | 2.0.0 finding; §13 CVD/accessibility content; §4 2.0 history |
| `curate-rev3-visual-tour` (C2) | §1.1–1.5 hero, flagship visuals, curated gallery, quality criteria (generate → review loop) |
| `publish-rev3-docs-site` (C3) | §2, §2.1 docs system, API reference, stale docs, deploy |
| `gate-release-artifacts-and-ci` (C4) | §5, §6 artifact gate, CI matrix; §12 PyPI measurement and decision |
| `finalize-public-api-contract` (C5) | §9 typing, §10 naming/API map, §13 output behavior |
| `prepare-3-0-release-notes` (C6) | §3 migration guide, §4 changelog, §7 README, §8 claims, §11 metadata |
| Release runbook (in `CONTRIBUTING.md`) | §4 final sequence, final closeout checklist |
| Roadmap 3.1+ backlog | §14–16 |

---

## C5 outcome: the public API contract (2026-09-19)

Archived. CI run #39 is green on all thirteen jobs, including the new
`Typing (pyright, public surface)` lane. 788 tests.

**`cp.Presets` is now `cp.PlotPresets`.** A deliberate break, taken now because published 2.0.0
never exposed `Presets` — it used `publication_preset()` — so no PyPI user is affected, and after
3.0 ships it would be a real one. C6 records it in the migration guide.

**The typing fix proved itself rather than being asserted.** `quick_plot` was annotated
`Callable[[complex], complex]` while every renderer calls the function with a numpy array, and the
package ships `py.typed`, so that annotation was a promise to every downstream type checker.
`tests/typing/downstream.py` uses the library the way a consumer does; with the old annotation
restored, pyright rejects three of its lines — an array-annotated user function, a
`TransferFunction`, and `preset.func` from the catalog. **The library's own objects did not satisfy
its own annotation.** With the `ComplexFunction` protocol, zero errors.

**The validation gap was larger than the proposal knew.** Beyond `phase_sectors` (`0` raised
`ZeroDivisionError` from inside a constructor; `-1` and `2.5` were accepted silently),
`Phase(r_log_base=1)` **rendered every pixel as NaN and raised nothing** — `log(x)/log(1)` divides
by zero, so the portrait came out blank with no error. `r_linear_step <= 0` and `scale_radius <= 0`
were accepted too. All are now checked in `BasePhasePortrait`, which covers `Phase` and the nine
perceptual families at once. A seventeen-probe sweep of the public surface now finds no bare Python
exceptions at all.

**The pyright gate is scoped on purpose.** It covers the public entry points and the downstream
fixture, not the whole package: that reports 86 errors, and the three most suspicious were each
checked and found to be false positives — two narrowing limitations (a compound `is None` guard,
and a variable set and read under the same `verbose` condition) and one pyvista stub imprecision
for a `tolerance` argument that genuinely exists. The whole-package count prints on every CI run
without blocking, so a jump stays visible without training anyone to add ignores.

**Left open for C6.** Whether `setup_matplotlib_backend` and `ensure_interactive_plots` belong in
`__all__` at all; they are environment helpers rather than visualization API, and C6 may retire
them rather than keep documenting them.

## C3 outcome: the documentation site (2026-09-18)

Archived. CI run #37 is green on all twelve jobs, including the new `Docs (mkdocs --strict)` lane.
The live 2.0 site is untouched: the deploy workflow has run exactly twice, both from `main` in
October 2025, and `gh-pages` still holds that build. Deployment now requires a `v3.*` tag or a
manual dispatch, so no branch push can publish.

**What shipped.** 26 navigated pages: the visual tour built from C2's approved captions, install
and first portrait, reading a phase portrait, domains, 3D and the Riemann sphere, Riemann surfaces,
engineering mode, catalog versus plot presets, the CLI, STL and physical output, a generated API
reference covering all 47 public names and all 5 in `cp.ee`, the catalog reference, the migration
guide, and contributing (which carries the C4 artifact gate). `mkdocs build --strict` runs on every
push; external links are checked weekly.

**Measurement corrected the writing.** The colormap page originally claimed `CubehelixPhase`
"increases monotonically in lightness" and recommended `DivergingWarmCool` for colour-vision
deficiency. Measuring the perceptual separation of twelve evenly spaced phases showed something
different, and more useful: `CubehelixPhase` is the only family that stays readable under both
deuteranomaly and greyscale, `Isoluminant` is the best in full colour and among the worst without
it, and **`DivergingWarmCool` and `EarthTopographic` fold the phase circle** — `φ` and `π − φ` come
out the same colour, so twelve phases give seven. That is by construction, not a defect, but it
means phase cannot be read off them. Two tests guard those claims.

**Decisions taken while implementing** (detail in the archived change's task 7): notebooks are
linked rather than embedded (26 MB of stored outputs, nothing linked them, and serving them broke
the strict build); `docs/pyvista_usage_guide.md` moved to `docs/internal/` because its install
section described the optional-PyVista world and its "15-30x" claim was on C6's list — so C6 has
one less file to fix; `docs/README.md` and `docs/cli.md` were removed in favour of `index.md` and
`guide/cli.md`, both having carried stale claims ("330+ tests", three subcommands).

**Left open.** Phone width is unverified — the browser tool's resize did not change the viewport,
and Material's responsiveness is a theme guarantee rather than something this change checked. The
built site is 77 MB, about 50 MB of it full-resolution gallery originals that the generated page
links as click-through targets; if that is too heavy for `gh-pages` history, the fix belongs in
`examples/showcase.py`. The CVD section links a notebook on `main`, which 404s until this branch
merges.

## Running the release gate locally

Moved to [CONTRIBUTING.md](../CONTRIBUTING.md), which C6 created for it: see "The release
artifact gate" and "Releasing". This tracker is a tracker again.

## C5 outcome: the public API contract (2026-09-19)

Archived. CI run #39 is green on all thirteen jobs, including the new
`Typing (pyright, public surface)` lane. 788 tests.

**`cp.Presets` is now `cp.PlotPresets`.** A deliberate break, taken now because published 2.0.0
never exposed `Presets` — it used `publication_preset()` — so no PyPI user is affected, and after
3.0 ships it would be a real one. C6 records it in the migration guide.

**The typing fix proved itself rather than being asserted.** `quick_plot` was annotated
`Callable[[complex], complex]` while every renderer calls the function with a numpy array, and the
package ships `py.typed`, so that annotation was a promise to every downstream type checker.
`tests/typing/downstream.py` uses the library the way a consumer does; with the old annotation
restored, pyright rejects three of its lines — an array-annotated user function, a
`TransferFunction`, and `preset.func` from the catalog. **The library's own objects did not satisfy
its own annotation.** With the `ComplexFunction` protocol, zero errors.

**The validation gap was larger than the proposal knew.** Beyond `phase_sectors` (`0` raised
`ZeroDivisionError` from inside a constructor; `-1` and `2.5` were accepted silently),
`Phase(r_log_base=1)` **rendered every pixel as NaN and raised nothing** — `log(x)/log(1)` divides
by zero, so the portrait came out blank with no error. `r_linear_step <= 0` and `scale_radius <= 0`
were accepted too. All are now checked in `BasePhasePortrait`, which covers `Phase` and the nine
perceptual families at once. A seventeen-probe sweep of the public surface now finds no bare Python
exceptions at all.

**The pyright gate is scoped on purpose.** It covers the public entry points and the downstream
fixture, not the whole package: that reports 86 errors, and the three most suspicious were each
checked and found to be false positives — two narrowing limitations (a compound `is None` guard,
and a variable set and read under the same `verbose` condition) and one pyvista stub imprecision
for a `tolerance` argument that genuinely exists. The whole-package count prints on every CI run
without blocking, so a jump stays visible without training anyone to add ignores.

**Left open for C6.** Whether `setup_matplotlib_backend` and `ensure_interactive_plots` belong in
`__all__` at all; they are environment helpers rather than visualization API, and C6 may retire
them rather than keep documenting them.

## C3 outcome: the documentation site (2026-09-18)

Archived. CI run #37 is green on all twelve jobs, including the new `Docs (mkdocs --strict)` lane.
The live 2.0 site is untouched: the deploy workflow has run exactly twice, both from `main` in
October 2025, and `gh-pages` still holds that build. Deployment now requires a `v3.*` tag or a
manual dispatch, so no branch push can publish.

**What shipped.** 26 navigated pages: the visual tour built from C2's approved captions, install
and first portrait, reading a phase portrait, domains, 3D and the Riemann sphere, Riemann surfaces,
engineering mode, catalog versus plot presets, the CLI, STL and physical output, a generated API
reference covering all 47 public names and all 5 in `cp.ee`, the catalog reference, the migration
guide, and contributing (which carries the C4 artifact gate). `mkdocs build --strict` runs on every
push; external links are checked weekly.

**Measurement corrected the writing.** The colormap page originally claimed `CubehelixPhase`
"increases monotonically in lightness" and recommended `DivergingWarmCool` for colour-vision
deficiency. Measuring the perceptual separation of twelve evenly spaced phases showed something
different, and more useful: `CubehelixPhase` is the only family that stays readable under both
deuteranomaly and greyscale, `Isoluminant` is the best in full colour and among the worst without
it, and **`DivergingWarmCool` and `EarthTopographic` fold the phase circle** — `φ` and `π − φ` come
out the same colour, so twelve phases give seven. That is by construction, not a defect, but it
means phase cannot be read off them. Two tests guard those claims.

**Decisions taken while implementing** (detail in the archived change's task 7): notebooks are
linked rather than embedded (26 MB of stored outputs, nothing linked them, and serving them broke
the strict build); `docs/pyvista_usage_guide.md` moved to `docs/internal/` because its install
section described the optional-PyVista world and its "15-30x" claim was on C6's list — so C6 has
one less file to fix; `docs/README.md` and `docs/cli.md` were removed in favour of `index.md` and
`guide/cli.md`, both having carried stale claims ("330+ tests", three subcommands).

**Left open.** Phone width is unverified — the browser tool's resize did not change the viewport,
and Material's responsiveness is a theme guarantee rather than something this change checked. The
built site is 77 MB, about 50 MB of it full-resolution gallery originals that the generated page
links as click-through targets; if that is too heavy for `gh-pages` history, the fix belongs in
`examples/showcase.py`. The CVD section links a notebook on `main`, which 404s until this branch
merges.

## Running the release gate locally (C4)

Until `CONTRIBUTING.md` exists (C6 writes it, and this section moves there), this is how to run the
artifact gate on a workstation before tagging. It is the same sequence the `artifact` job runs, and
it is worth running once by hand because it exercises what a user installs rather than the working
tree.

```bash
# 1. Build, and check the metadata the way PyPI will.
uv build
uv run --no-project --with twine twine check dist/*

# 2. Inspect the built distributions: contents, licences, py.typed, metadata, entry point.
python scripts/check_distribution.py dist

# 3. Install the WHEEL into a throwaway environment - not the checkout.
uv venv /tmp/smoke --python 3.12
uv pip install --python /tmp/smoke/bin/python dist/*.whl

# 4. Smoke it from somewhere else: import, CLI, 2D render, off-screen 3D render, STL export.
#    The script refuses to run against a source tree, because that is the mistake it exists to
#    catch. On Windows use /tmp/smoke/Scripts/python.exe.
cd /tmp && /tmp/smoke/bin/python "$OLDPWD/scripts/smoke_wheel.py"
```

The rest of the gate is the ordinary suite: `pytest tests/ -q`, `ruff check` and
`ruff format --check` over `complexplorer/ tests/ examples/ scripts/`, `openspec validate --specs`,
and — before a release, not on every push — `pytest --nbmake examples/notebooks/`.

**Outcome (2026-09-17).** C4 is archived. CI run #35 is green on all eleven jobs: lint, Linux and
Windows on 3.11/3.12/3.13, macOS on 3.12, the minimum-dependency lane, the non-blocking 3.14 lane,
and the artifact job (build, twine, inspect, wheel install, five-check smoke, sdist to wheel).

**What the gate caught, over three runs.** Four defects, none of which the previous CI could see,
because it only ever tested the source tree on two platforms:

1. `complexplorer list` died with `UnicodeEncodeError` on a stock `cmd.exe` (cp437) printing the
   preset title `z³ - z` — the first command in the quickstart, on Windows.
2. An STL export died the same way on cp1252, printing a `✗` marker — after building the mesh
   and before writing the file, so the work was lost and the user got a traceback.
3. `index.json` was not byte-stable across platforms: a derived coordinate was
   `-0.8090169943749475` on Windows and `-0.8090169943749476` on Linux. The serialized record is
   now quantized, and the capability promises identical manifests on any platform.
4. Two knife-edge tests: the manifest comparison could not survive git's CRLF conversion in a
   Windows working tree (now pinned in `.gitattributes`), and `sawtooth_log(e)` was asserted at a
   discontinuity, which an older libm falls the other side of (found by the new
   minimum-dependency lane on its first run).

Only the first two are user-facing bugs; the rest are contract and test fragility. All four were
invisible to a suite that passes 695 tests locally.

**One decision left open: the Python 3.14 classifier.** Task 4.4 says to add it once the lane is
green, and the lane passed on its first run. It is deliberately not added yet, for two reasons: one
green run on a lane designed to be unreliable is not evidence of support, and claiming 3.14 on PyPI
while its lane cannot fail the build would contradict the matrix requirement this change just
established. Claiming it means also making that lane blocking. Worth revisiting at tag time.

**What the gate caught on its first run (2026-09-17).** Two shipped defects, both
`UnicodeEncodeError` on a Windows console using a legacy code page, both fixed in C4 with
regression tests in `tests/unit/test_console_encoding.py`:

- `complexplorer list` died on the preset title `z³ - z` under cp437, the default in a stock
  `cmd.exe`. That is the first command in the quickstart.
- An STL export died on a `✗` status marker under cp1252 — after building the mesh, before
  writing the file.

Neither was reachable from the test suite, which never encodes output for a console. This is the
argument for the gate: the tests cover the library, the gate covers the product.

---

# P0 — complete before release

## 1. Make the visual showcase a release gate

Complexplorer is a visualization library. The images are not decorative documentation; they
are primary evidence that the package works and the fastest explanation of why it exists.

The current generated gallery is a strong asset base:

- 17 phase portraits
- 7 analytic landscapes
- 7 Riemann spheres
- 3 multivalued Riemann surfaces
- 2 ornament/relief renders
- 8 colormap examples

However, that asset count overstates the breadth currently *shown*. Several rev3 headline
capabilities are absent from the gallery, and the generated page presents many full-size images
in a long vertical stream rather than as a curated tour.

### 1.1 Create a “rev3 at a glance” hero montage

- [ ] Replace or supplement the current single hero image with a clean 2×3 montage.
- [ ] Keep it readable at normal GitHub README width; avoid tiny labels and large unused margins.
- [ ] Use one visual for each distinct idea rather than six variants of the same function.
- [ ] Use consistent backgrounds, crops, typography, camera conventions, and output dimensions.
- [ ] Include concise labels in the montage or an immediately adjacent caption.
- [ ] Verify the montage renders correctly on GitHub and the PyPI project page.

Recommended panels:

| Panel | Capability to prove | Suggested content |
|---|---|---|
| 1 | Domain coloring | `(z² - 1)/(z² + 1)` with the phase-wheel legend visible |
| 2 | Analytic landscape | The same function, making the 2D → 3D relationship obvious |
| 3 | Riemann sphere or relief | `z/(z¹⁰ - 1)` or another visually distinctive full-sphere render |
| 4 | Multivalued surface | `sqrt(z)` or `log(z)`, with a view that clearly exposes sheet structure |
| 5 | Engineering mode | Transfer-function portrait with poles, zeros, stability boundary, and legend |
| 6 | Math to matter | Rendered ornament beside a photograph of the printed object |

The montage should communicate the umbrella proposition: **visual exploration of complex
mappings—from domain coloring and multivalued geometry to transfer functions and physical
objects.** Keep “transform complex mathematics into tangible art” as the secondary tagline.

### 1.2 Add the missing flagship visuals

- [ ] **Phase-wheel legend:** generate at least one committed portrait with `legend=True`.
      The legend is a rev3 feature and currently is described but not visibly demonstrated.
- [ ] **Engineering mode:** add a compact showcase for one nontrivial stable transfer function.
      At minimum show `transfer_portrait`; ideally present the portrait, pole-zero map, Bode
      plot, and Nyquist plot as one coherent figure or tightly grouped set.
- [ ] **Composition proof:** show that the same `TransferFunction` object works in a standard
      Complexplorer renderer, preferably a transfer portrait beside a 3D landscape.
- [ ] **Composite domains:** add one image whose geometry visibly depends on union,
      intersection, or difference. The feature is currently claimed but not shown.
- [ ] **Riemann surface versus Riemann sphere:** add a side-by-side explanation. The current
      prose distinction is good, but the visual distinction should be immediate.
- [ ] **STL to physical print:** show the generated relief, mesh/STL view, and a real printed
      object. This is the strongest evidence for the “math to matter” identity.
- [ ] **Interactivity:** produce one short, restrained loop of a rotating Riemann surface or
      relief. A static screenshot cannot prove rotation, zoom, lighting, or depth perception.
      Use this in the documentation/release post; embed it in the README only if size and
      loading behavior remain reasonable.

### 1.3 Turn the gallery into a curated visual tour

- [ ] Add a compact table of contents or thumbnail grid at the top of the gallery.
- [ ] Organize by idea rather than registry order: phase portraits; mapping and topology;
      Riemann surfaces; engineering; colormaps; physical output.
- [ ] Use thumbnail/card views that link to full-resolution images. Do not make readers scroll
      through roughly 45 full-size images to discover the range.
- [ ] Give each flagship figure a one- or two-sentence interpretation: what mathematical
      structure is visible and what rendering mode reveals it.
- [ ] Keep runnable code adjacent to the corresponding visual, but collapse or de-emphasize
      repetitive boilerplate.
- [ ] Add useful alt text. Current generated alt text often repeats only the preset title and
      does not distinguish portrait, landscape, sphere, surface, or ornament.
- [ ] Ensure every generated page clearly says which source file to edit and which command
      regenerates it.
- [ ] Add engineering, composite-domain, and phase-wheel assets to the presentation manifest,
      even if they do not naturally belong in `cp.catalog`.

### 1.4 Establish visual quality criteria

- [ ] Define canonical camera, background, lighting, axes, window size, and antialiasing
      settings for each PyVista render family.
- [ ] Inspect the sphere seam/poles and Riemann-surface intersections at full resolution.
- [ ] Check color fidelity and contrast in both light and dark browser themes.
- [ ] Confirm labels and legends remain legible after README/PyPI scaling.
- [ ] Avoid clipped peaks, excessive empty margins, unreadable orientation widgets, and views
      where sheet structure collapses into a flat silhouette.
- [ ] Record the function, domain, colormap, scaling, resolution, and camera used for each
      curated asset.
- [ ] Decide which images are deterministic build outputs and which are hand-curated release
      assets. The `index.json` manifest is byte-stable; PNGs are currently best-effort and
      should not be described as byte-identical.
- [ ] Regenerate the gallery from a clean checkout and review the actual diff before tagging.

### 1.5 Create a short visual narrative for the release

A useful five-step story is:

1. **See a complex function** as a phase portrait.
2. **Read the image** using the phase-wheel legend.
3. **Lift magnitude into geometry** as a landscape or Riemann relief.
4. **Resolve multivalued behavior** on a Riemann surface, or apply the same machinery to an
   engineering transfer function.
5. **Export the geometry** as an STL and make it physical.

- [ ] Build the documentation landing page and release announcement around this sequence.
- [ ] Ensure each step uses a minimal public-API snippet that runs as printed.
- [ ] Do not make the catalog, CLI, or surface kernel compete with the visual story. Present
      them as the machinery that makes the story reproducible and composable.

## 2. Replace the current documentation gap with a small, complete rev3 set

The repository has useful notebooks and Markdown guides, but no visible documentation build
configuration, no API-reference generator, and no rev3 deployment workflow. The live docs must
not continue to describe v2 when 3.0 is released.

- [ ] Choose and configure one documentation system (MkDocs + mkdocstrings or Sphinx are both
      reasonable). Optimize for good image presentation and generated API reference.
- [ ] Add a docs build check in CI, including warnings-as-errors where practical.
- [ ] Add link checking for internal links and the small set of essential external links.
- [ ] Add a deployment workflow and make documentation deployment part of the release sequence.
- [ ] Ensure the hosted docs display the installed package version or clearly label an
      unreleased/dev build.
- [ ] Add an actual API reference generated from the public package. “Use `help()`” is not an
      adequate API-reference strategy for rev3.

Minimum documentation set:

- [ ] **Overview / visual tour** — the curated five-step story above.
- [ ] **Installation and first portrait** — a result within five minutes.
- [ ] **Reading a phase portrait** — hue, winding, modulus shading, zeros, poles, and the
      phase-wheel legend.
- [ ] **Domains and colormaps** — including a composite-domain example.
- [ ] **3D landscapes and Riemann sphere/relief** — camera, screenshots, headless use, and
      the supported PyVista workflow.
- [ ] **Riemann surfaces** — `power`, `log`, and algebraic families; branch points/cuts; sphere
      versus surface.
- [ ] **Engineering mode** — transfer portrait, pole-zero, Bode, Nyquist, and composition with
      general renderers.
- [ ] **Catalog and plot presets** — explicitly distinguish `cp.catalog` from `cp.Presets`.
- [ ] **CLI** — render, STL, list, and gallery, with output examples.
- [ ] **STL export / physical workflow** — sizing, watertightness, print caveats, and a physical
      result.
- [ ] **API reference** — all supported public names and signatures.
- [ ] **2.x → 3.0 migration guide**.
- [ ] **Contributing / development** — local setup and release checks.

### 2.1 Fix specific stale documentation already present

- [ ] `docs/cli.md`: remove the claim that 2D/list work without the 3D backend and that users
      install `complexplorer[pyvista]`; PyVista is now a required dependency.
- [ ] `docs/development/backend-policy.md`: Riemann surfaces are no longer “future,” the surface
      kernel is no longer “forthcoming,” and matplotlib 3D is no longer merely “frozen pending
      removal.”
- [ ] `docs/development/surface-kernel.md`: remove references to legacy projection helpers being
      “slated for removal.” Reconcile the statement that core/2D remains importable without the
      3D backend with the package's unconditional top-level PyVista imports.
- [ ] `examples/README.md`: remove “added in a follow-up change”; the high-resolution showcase
      script already exists.
- [ ] `docs/README.md`: replace the generic “recent updates” list with rev3 headlines and remove
      or dynamically generate the `330+ tests` claim.
- [ ] `docs/function-presets.md`: move the Godot/game-boundary and future `FunctionFamily`
      discussion to internal design notes unless it is intentionally part of the user-facing
      product story.
- [ ] `complexplorer/core/presets.py` and `complexplorer/gallery.py`: rewrite public module
      docstrings around the stable catalog/gallery contract; remove roadmap-phase and future
      consumer language.
- [ ] Review `docs/phase_7_physical_texture_plan.md` and other implementation plans. Move them
      under an explicitly internal/architecture area or remove them from the published nav.

## 3. Write the 2.x → 3.0 migration guide

The changelog contains the raw material, but a migration guide should be organized around user
actions rather than the history of implementation.

- [ ] Start with a compact “if you only do three things” section.
- [ ] Show old → new imports/calls for removed matplotlib 3D entry points:
      `plot_landscape` → `plot_landscape_pv`, `pair_plot_landscape` →
      `pair_plot_landscape_pv`, and 3D `riemann` → `riemann_pv`.
- [ ] Explain that PyVista/VTK is now installed as a required dependency and the old capability
      flags are gone.
- [ ] Document renamed renderer arguments: `n_theta`/`n_phi` → `resolution`, `show` →
      `interactive`, and removal of `project_from_north`.
- [ ] Explain the non-square `Rectangle.contains()` correction and where changed masks/STLs may
      affect output.
- [ ] List removed high-level stubs and aliases (`create_animation`, `compare_functions`,
      `analyze_function`, `visualize`, `explore`) with replacements or an explicit “no direct
      replacement.”
- [ ] List the remaining removed public/internal symbols in a compact compatibility table.
- [ ] Include one complete before/after example and a short troubleshooting section.
- [ ] Link the guide from the README, changelog, docs home, and GitHub release notes.

## 4. Make version and publication state truthful

- [ ] Change the pending changelog entry from `## [3.0.0] - 2026-07-04` to `Unreleased` or
      `## [3.0.0] - Unreleased` until the release date is known.
- [ ] Verify the history statement for 2.0. The changelog says 2.0 was tagged but never
      published, while the public package history should be checked and described accurately.
- [ ] Add compare links for `[Unreleased]`, `[3.0.0]`, and prior releases at the bottom of the
      changelog.
- [ ] Do not publish a README that points to rev3 documentation before that documentation is
      live, or a package that claims rev3 while the linked documentation is still v2.
- [ ] Final sequence: merge to `main` → final clean-checkout gate → tag `v3.0.0` → publish
      wheel/sdist → deploy docs → create GitHub release.
- [ ] Verify the PyPI landing page after upload: hero image, badges, links, code fences,
      license metadata, and long-description rendering.

## 5. Enforce an artifact-level release gate

The current CI installs the source editable with `uv pip install -e ".[dev]"`. The hardening
OpenSpec records a successful one-time wheel smoke test, but this is not yet enforced in the
workflow.

- [ ] Add a CI job that runs `python -m build` (or `uv build`).
- [ ] Run `twine check dist/*` or the equivalent metadata validation.
- [ ] Create a fresh environment and install the built wheel, not the working tree.
- [ ] From the installed wheel, run:
  - [ ] `import complexplorer as cp; print(cp.__version__)`
  - [ ] `complexplorer list`
  - [ ] one 2D file render
  - [ ] one real off-screen PyVista landscape or sphere render
  - [ ] one small STL export
- [ ] Inspect wheel/sdist contents: `py.typed`, both license files, expected subpackages, and no
      examples/tests/build debris.
- [ ] Inspect wheel metadata: version, `Requires-Python`, dependencies, license expression,
      classifiers, URLs, and console entry point.
- [ ] Test the sdist path at least once by building a wheel from the sdist in a clean
      environment.

## 6. Align CI with the compatibility claims

- [ ] Remove `continue-on-error` from Python 3.13 or remove the 3.13 classifier. A claimed Python
      version must block the release when it fails.
- [ ] Add one macOS lane, especially because PyVista/VTK is required.
- [ ] Add an Ubuntu/Python 3.11 minimum-dependency job that installs the declared lower bounds:
      NumPy 1.26, matplotlib 3.8, SciPy 1.11, asteval 1.0, and PyVista 0.47.
- [ ] Keep the ordinary matrix on current dependencies to catch upstream breakage.
- [ ] Add notebook execution to CI or a required pre-release workflow. The notebooks are core
      onboarding material, not incidental examples.
- [ ] Lint/test the executable examples. Ruff currently excludes the entire `examples/` tree,
      including the gallery producer and interactive showcase.
- [ ] Add a gallery structural check that regenerates into a temporary directory and verifies
      manifest/schema consistency without requiring PNG byte identity.
- [ ] Consider scheduled CI against newest dependencies so an upstream VTK/PyVista change is
      detected before a release sprint.

---

# P1 — high-value polish

## 7. Reframe the README around visual exploration

- [ ] Replace the art-only opening with an umbrella description, for example:

  > Complexplorer is a Python toolkit for exploring complex functions visually—from domain
  > coloring and Riemann surfaces to transfer functions and 3D-printable mathematical geometry.

- [ ] Retain “Transform complex mathematics into tangible art” as a memorable secondary
      tagline.
- [ ] Add a short “What’s new in 3.0” strip immediately after the hero:
      **Riemann surfaces · engineering mode · CLI and catalog · phase-wheel legend · unified
      PyVista 3D**.
- [ ] Put one minimal 2D example before the more specialized ornament workflow. A new user should
      get visible output with the smallest possible code snippet.
- [ ] Keep the advanced examples, but move long explanations of modulus modes, domain
      restrictions, and printing below the visual tour or into docs.
- [ ] Add a prominent migration-guide link.
- [ ] Review emoji density and headings for a more consistent scientific-tool identity.

## 8. Remove or substantiate brittle marketing claims

- [ ] Replace “first library to…” claims unless priority can be documented responsibly.
- [ ] Replace “15–30x faster” with “high-performance” unless a reproducible benchmark, baseline,
      hardware description, and tested version range are published.
- [ ] Replace “cinema-quality output” with a concrete capability such as configurable lighting,
      antialiasing, high-resolution screenshots, or interactive navigation.
- [ ] Replace “no supports needed” with “designed for FDM printing” plus tested orientation,
      minimum thickness, and support caveats. Printability depends on function, geometry,
      printer, material, and slicer settings.
- [ ] Audit the README, changelog, PyVista guide, package metadata, and release announcement so
      softened wording is consistent everywhere.

## 9. Finish the public typing contract

Shipping `py.typed` invites downstream type checkers to rely on annotations. Concentrate on the
public API rather than attempting to type every implementation detail.

- [ ] Introduce a canonical type/Protocol for vectorized complex functions; current
      `Callable[[complex], complex]` on `quick_plot` does not describe functions called with
      NumPy arrays.
- [ ] Type `FunctionPreset.func`, domain/colormap/scaling specs, singularity records, and catalog
      return values more precisely.
- [ ] Add explicit public return types for `quick_plot`, `Presets` methods, engineering plotting
      functions, CLI resolution helpers that are part of tested behavior, and gallery APIs.
- [ ] Replace broad bare `dict`, `tuple`, and `Callable` annotations on public contracts with
      aliases, `TypedDict`, or Protocols where that improves user feedback.
- [ ] Decide how scalar-versus-array return behavior of `TransferFunction.__call__` should be
      represented and document it.
- [ ] Add a small Pyright or mypy gate over the package's public surface.
- [ ] Add one downstream-style typing fixture that imports the installed wheel.

## 10. Resolve naming and API discoverability

- [ ] Make a final explicit decision on `cp.Presets` versus `cp.catalog`. The distinction is
      defensible, but it is not immediately obvious.
- [ ] If preserving compatibility, keep the names and teach the distinction everywhere with one
      sentence: **`Presets` configures a render; `catalog` supplies a function.**
- [ ] If renaming `Presets` to `PlotPresets`, do it now, during the major release, with a clear
      migration note. Do not carry both names indefinitely without a deprecation policy.
- [ ] Add a compact top-level API map to the docs so users can find the recommended entry point
      for 2D, 3D, sphere, surface, engineering, gallery, and STL workflows.
- [ ] Confirm every name in `complexplorer.__all__` is documented, typed, and intentionally
      public.
- [ ] Clarify return contracts (`Axes`, `Figure`, `Plotter`, mesh, path, or `None`) across the
      renderer docs.

## 11. Add professional repository metadata

- [ ] Add `CONTRIBUTING.md`: environment setup, editable install, tests, Ruff, OpenSpec workflow,
      notebook execution, gallery regeneration, and artifact checks.
- [ ] Add `CITATION.cff` so GitHub exposes “Cite this repository” with structured authorship and
      version metadata.
- [ ] Replace the README's hard-coded `year = {2024}` citation or generate the BibTeX guidance
      from the CFF metadata.
- [ ] Expand `[project.urls]` with Documentation, Source, Changelog, and Release notes.
- [ ] Add a support route: GitHub Discussions, Issues, or a short support policy.
- [ ] Add issue templates for bug reports and visual/rendering problems; collect OS, Python,
      PyVista/VTK versions, backend, GPU/display context, and a minimal function/domain example.

## 12. Revisit the mandatory PyVista decision once, then close it

PyVista/VTK is now installed for every user, including those who only want a 2D phase portrait.
The architectural benefit is real, but the dependency and import cost are material.

- [ ] Record the decision explicitly: required PyVista versus an optional `3d` extra with lazy
      imports.
- [ ] Measure wheel/install size and cold `import complexplorer` time on at least one clean
      environment.
- [ ] Test installation on the three supported operating systems.
- [ ] If PyVista remains required, explain the rationale once in the backend policy and remove
      all residual optional-backend language.
- [ ] If it becomes optional, restore the boundary cleanly rather than reviving scattered
      `HAS_PYVISTA` flags.

This is the highest-cost architectural choice to reverse later; it deserves a conscious final
decision, not repeated uncertainty after release.

## 13. Polish accessibility and output behavior

- [ ] Document whether the default phase palettes are interpretable under common color-vision
      deficiencies and offer at least one accessible alternative or caveat.
- [ ] Ensure legends do not obscure important singularities or get clipped when saving figures.
- [ ] Verify transparent and white-background export behavior for publication use.
- [ ] Standardize figure titles, axis labels, mathematical typography, and DPI guidance.
- [ ] Document how users obtain deterministic camera views and reproducible screenshots.
- [ ] Confirm warnings and validation errors name the invalid value, accepted values, and the
      relevant replacement when migrating from v2.

---

# P2 — useful after 3.0, not release blockers

## 14. Release and maintenance automation

- [ ] Add trusted publishing from GitHub Actions to PyPI after the manual release path is proven.
- [ ] Add a TestPyPI dry run for release candidates.
- [ ] Add a manually triggered release workflow that builds once and promotes the same tested
      artifacts.
- [ ] Generate draft GitHub release notes from the curated changelog/migration guide.
- [ ] Add documentation previews for pull requests that change public behavior or visuals.
- [ ] Add image-size budgeting so the README/docs remain responsive.

## 15. Community/repository polish

- [ ] Add a concise code of conduct if external contributions begin to arrive.
- [ ] Add `SECURITY.md` if the safe expression evaluator or CLI broadens the threat model.
- [ ] Add ownership/review rules if the contributor base grows.
- [ ] Consider DOI/Zenodo archival for citable releases after `CITATION.cff` is in place.

## 16. Future feature backlog—explicitly defer from rev3

Do not delay 3.0 for these:

- animation APIs such as `create_animation`
- general function comparison APIs
- automatic singularity detection
- additional Riemann-surface families
- additional transfer-function analysis
- new colormaps or scaling modes
- a parameterized `FunctionFamily` system
- game/Godot consumers of catalog data
- deeper STL texture work

Capture them as 3.x proposals after the public 3.0 contract is stable.

---

# Final closeout checklist

All six changes (C1-C6) are archived, plus `honour-pyvista-off-screen`, which the C6 work turned
up. The items below are ticked against what was actually verified; the release-time items are
deliberately left open, because they cannot be true until the tag is cut.

## Content and visuals

- [x] Rev3 hero montage approved at README scale. (C2 round R2; the chosen `labels_below` variant.)
- [x] Phase-wheel, engineering, composite-domain, Riemann-surface, and physical-print visuals
      present. (Eight tour recipes, all approved in rounds R1-R4.)
- [x] Gallery has navigation/thumbnails, useful captions, accurate alt text, and runnable code.
- [x] One motion asset demonstrates PyVista interactivity. (`_tour/orbit_loop.gif`, budgeted
      under 3.5 MB.)
- [x] All committed generated assets reproduce from documented commands.
      (`python examples/showcase.py`; manifests are byte-identical on any platform since C4.)
- [x] README and docs screenshots reviewed on GitHub and PyPI. **Partly:** the README was
      reviewed at GitHub width in C2 round R4 and its long description passes `twine check` with
      every link absolute. The live PyPI page for 3.0 cannot be reviewed until it is published --
      see the release-time list below.

## Documentation and API

- [x] Hosted rev3 docs complete and ready to deploy. (26 pages; `mkdocs build --strict` is a CI
      gate. Deployment is tag-triggered, so the 2.0 site stays live until 3.0 ships.)
- [x] API reference generated and complete. (mkdocstrings over all 45 public names and the 5 in
      `cp.ee`; a test fails if one is missing or invented.)
- [x] Migration guide linked from every relevant entry point. (README, changelog, docs nav, and
      the troubleshooting entries that name specific errors.)
- [x] Stale backend, roadmap-phase, and internal consumer language removed. (The Godot and
      `FunctionFamily` material moved to `docs/internal/`; the superseded PyVista usage guide with
      it.)
- [x] Public API naming and mandatory-PyVista decisions recorded. (`PlotPresets` in C5; the
      backend policy carries the measured footprint and import cost.)
- [x] Public annotations pass the selected type checker. (pyright over the public surface and a
      downstream fixture, gated in CI.)

## Quality and artifacts

- [x] `ruff check` and `ruff format --check` over `complexplorer/ tests/ examples/ scripts/
      docs/hooks/` (wider than this line originally asked for).
- [x] full `pytest` suite on the supported Python/OS matrix. (824 tests; Linux and Windows on
      3.11/3.12/3.13, macOS on 3.12.)
- [x] real off-screen PyVista render tests. (In CI on Linux under a headless display, and in the
      wheel smoke test on every run.)
- [x] `pytest --nbmake examples/notebooks/`. (Confirmed by the author locally; the Notebooks
      workflow registers with GitHub once this branch reaches the default branch.)
- [x] `openspec validate --specs` (20 capabilities).
- [x] clean gallery regeneration and reviewed diff. (C2 round R3, with the R4 in-context pass.)
- [x] wheel/sdist build and metadata validation. (`twine check` plus
      `scripts/check_distribution.py`.)
- [x] fresh-environment wheel install and CLI/render/STL smoke tests.
      (`scripts/smoke_wheel.py`, which refuses to run against a source tree.)
- [x] minimum-dependency and Python 3.13 gates are blocking.
- [x] one macOS test lane passes.

## Release state

Everything above is done. These five are the release itself, and are the content of
`CONTRIBUTING.md` -> "Releasing":

- [ ] Changelog uses the actual release date. (It says `## [3.0.0] - Unreleased` on purpose.)
- [ ] `complexplorer.__version__`, tag, wheel metadata, docs, and release title agree.
- [ ] README claims are evidence-backed or softened. **Done ahead of the tag:** the "15-30x",
      "cinema-quality", "first library", "no supports needed" and "ultimate visualization" claims
      are gone, and a test fails if any returns.
- [x] `CONTRIBUTING.md`, `CITATION.cff`, and project URLs are present.
- [ ] Release performed in the agreed order and PyPI/docs verified from a clean browser session.

## Open decisions carried past the closeout

- **The Python 3.14 classifier stays off.** The lane has been green for several runs but is
  non-blocking; claiming support while it cannot fail the build would contradict the matrix
  requirement C4 established. Revisit by making that lane blocking and adding the classifier
  together.
- **The built site is 77 MB**, about 50 MB of it full-resolution gallery originals that the
  generated page links as click-through targets. Only a concern because each deploy commits into
  `gh-pages` history; the fix would be in `examples/showcase.py`.
- **Phone-width rendering of the docs site is unverified.** Material for MkDocs is responsive by
  default, but this closeout did not confirm it on a device.

## Recommended effort allocation

- **45% visual showcase and documentation**
- **25% packaging, CI, and release mechanics**
- **20% public API, typing, and metadata polish**
- **10% cleanup and final verification**

The release should remain feature-frozen during closeout. The codebase already contains the
intellectual substance of rev3; the final work is to make that substance visible, teachable,
and dependable.
