## 1. Make the scaffold build

- [ ] 1.1 Add a `docs` extra to `pyproject.toml`: `mkdocs-material`, `mkdocstrings[python]`,
  `mkdocs-jupyter`, pinned to a minor the way ruff is, so the gate cannot break on a release of
  someone else's tool.
- [ ] 1.2 Rewrite `mkdocs.yml`: the rev3 nav, `strict: true`, `exclude_docs` covering
  `internal/`, and the `mkdocstrings` and `hooks` configuration.
- [ ] 1.3 Add `docs/hooks/site_assets.py` with an `on_files` hook that adds `examples/gallery/**`
  and `examples/notebooks/*.ipynb` to the file set at their repository-relative destinations, so
  the generated gallery page's existing links resolve without rewriting it.
- [ ] 1.4 Add the version hook and the `main.html` override that renders the announcement bar:
  the installed `complexplorer.__version__`, marked as a development build unless `HEAD` is a
  `v*` tag.
- [ ] 1.5 Confirm `mkdocs build --strict` passes on the scaffold before any page is written, so a
  later failure is about content rather than configuration.

## 2. The page set

- [ ] 2.1 `index.md` — overview and visual tour, built on the C2 hero and tour assets: what the
  library is for, in the order the closeout's story goes.
- [ ] 2.2 Installation and a first portrait, ending with a rendered image the reader just produced.
- [ ] 2.3 Reading a phase portrait: phase to hue, the enhanced portrait, the `legend=True` wheel,
  and the colour-vision-deficiency guidance ported in C1.
- [ ] 2.4 Domains and colormaps, including composite domains built with `|`, `&` and `-`, and the
  perceptual family restored in C1.
- [ ] 2.5 3D landscapes and the Riemann sphere and relief: headless use, a reproducible camera, and
  writing an image to a file.
- [ ] 2.6 Riemann surfaces — the multi-sheeted cover, and how it differs from the sphere.
- [ ] 2.7 Engineering mode (`cp.ee`): transfer portrait, pole-zero, Bode, Nyquist, and the point
  that a `TransferFunction` is a plain callable every renderer accepts.
- [ ] 2.8 Catalog versus plot presets, in one page that makes the distinction stick.
- [ ] 2.9 The CLI: all four subcommands, `gallery` included.
- [ ] 2.10 STL and the physical workflow, using the C2 ornament photographs.
- [ ] 2.11 The API reference pages, grouped by workflow, generated by `mkdocstrings`.
- [ ] 2.12 Contributing and release checks, including how to run the C4 artifact gate locally
  (move the section out of `openspec/REV3_CLOSEOUT.md` when C6 creates `CONTRIBUTING.md`).
- [ ] 2.13 Wire the generated gallery page and the notebooks into the nav.

## 3. Correct the stale documentation

- [ ] 3.1 `docs/cli.md`: document the `gallery` subcommand, and remove the claim that some
  commands work without the 3D backend — PyVista has been required since 3.0.
- [ ] 3.2 `docs/README.md`: "330+ tests" is wrong; the suite collects 695. Check every other
  countable claim on the page while there.
- [ ] 3.3 `docs/development/surface-kernel.md` and `examples/README.md`: check against the 3.0
  surface and correct what has drifted.
- [ ] 3.4 Module docstrings in `complexplorer/core/presets.py` and `complexplorer/gallery.py`.
- [ ] 3.5 Move the Godot and `FunctionFamily` material out of `docs/function-presets.md` into
  `docs/internal/`: it describes a consumer that does not exist.
- [ ] 3.6 Move `docs/phase_7_physical_texture_plan.md` and
  `docs/composite_domain_viewing_window_fix.md` to `docs/internal/`.

## 4. CI and deploy

- [ ] 4.1 Add a `docs` job to CI running `mkdocs build --strict` on every push.
- [ ] 4.2 Add the external-link check to the weekly scheduled run, not to the per-push job.
- [ ] 4.3 Rewrite `.github/workflows/docs.yml` to deploy on a `v3.*` tag only, keeping manual
  dispatch, and confirm no branch push can publish.

## 5. Tests

- [ ] 5.1 A test asserting every name in `complexplorer.__all__` and `cp.ee` appears in an API
  reference page, so grouping by hand cannot drop one.
- [ ] 5.2 A test asserting every page in the `mkdocs.yml` nav exists on disk — a fast failure that
  does not need a full build.
- [ ] 5.3 A test asserting the documented CLI subcommands match `build_parser()`, so `docs/cli.md`
  cannot fall behind the program again.

## 6. Verification

- [ ] 6.1 `mkdocs build --strict` clean; `pytest`, `ruff check`/`format`, `openspec validate --specs`
  and `openspec validate publish-rev3-docs-site` all green.
- [ ] 6.2 Serve the built site and walk it: the tour, the gallery images, a notebook, the API
  reference, and the version banner. Check it at phone width.
- [ ] 6.3 Confirm on CI that the docs job passes and that no push deployed anything; the live 2.0
  site is untouched.
- [ ] 6.4 Flip C3's status in `openspec/ROADMAP.md` and record the outcome in
  `openspec/REV3_CLOSEOUT.md`.
